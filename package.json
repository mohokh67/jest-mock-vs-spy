{
    "name": "testing-with-jest",
    "version": "1.0.0",
    "main": "dist/index.js",
    "module": "dist/index.mjs",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "pnpm run lint && tsup src/**/*.ts --format cjs,esm --dts",
        "clean": "rimraf dist",
        "lint": "tsc",
        "test": "jest --detectOpenHandles --colors --runInBand --verbose"
    },
    "dependencies": {
    },

    "devDependencies": {
        "@types/jest": "^29.5.0",
        "@types/node": "^18.15.3",
        "jest": "^29.5.0",
        "rimraf": "^4.4.0",
        "ts-jest": "^29.0.5",
        "tsup": "^6.6.3",
        "typescript": "^4.9.5"
    },
    "packageManager": "pnpm@7.0.0"
}
